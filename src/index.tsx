import React, { memo, ReactElement } from 'react';

// interface IHeatmapProps {}

function Heatmap(/* props?: IHeatmapProps */): ReactElement {
  // console.log(props);
  return <div>TODO develop Heatmap component</div>;
}

export default memo(Heatmap);
